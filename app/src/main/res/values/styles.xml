<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="actionMenuTextColor">@color/primary_light</item>
        <item name="android:actionButtonStyle">@style/ButtonStyle</item>
        <item name="actionButtonStyle">@style/ButtonStyle</item>
    </style>

    <style name="CropStyle" parent="Theme.AppCompat">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/primary_dark</item>
        <item name="colorAccent">@color/colorPrimary</item>
        <item name="actionMenuTextColor">@color/icons</item>
    </style>

    <style name="ButtonStyle" parent="Widget.AppCompat.ActionButton">
        <item name="android:minWidth">50dp</item>
        <item name="android:maxWidth">20dp</item>
        <item name="android:width">24dp</item>
    </style>

</resources>
